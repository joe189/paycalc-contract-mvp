PORT=3000
ADMIN_PASSWORD=change-me
PAYCALC_DB_PATH=./data/paycalc.sqlite

# Optional: point this to a small internal endpoint that accepts ?jobId=12345
# and returns JSON with fields like contractorName, jobTitle, facilityName,
# facilityCity, facilityState, startDate, endDate, schedule, shift, billRate,
# hoursPerWeek, onCallBillRate, and payType.
JOBDIVA_LOOKUP_URL=
JOBDIVA_API_TOKEN=
